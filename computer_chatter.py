import serial
from time import time, sleep

# SETUP
pass

# LOOP
try:
    while True:
        # Transmit data (TX)
        pass
        if usb_messenger.in_waiting > 0:
        # Receive data (RX)
                pass

except KeyboardInterrupt:
    pass
